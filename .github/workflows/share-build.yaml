on:
  workflow_call:
    inputs:
      username:
        required: false
        type: string
    secrets:
      envPAT:
        required: false

jobs:
  share_build_workflow:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
        with:
          repository: cocdibui/actions
          token: ${{ secrets.GIT_PAT }}
          path: coinmap-actions
      - uses: ./coinmap-actions/build

